const { MessageEmbed } = require('discord.js');

module.exports = {
    config: {
        name: '',
        description: '',
    },
    run: async (client, interaction, args) => {

       
    }
}